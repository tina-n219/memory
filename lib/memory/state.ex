defmodule Memory.State do
    # def new do 
    #     %{
    #         board: gen_board(),
    #         flipped: [],
    #         score: 0
    #     }
    # end

    # def gen_board do
    #     board = ['a', 'a', 'b', 'b', 'c', 'c', 'd', 'd', 'e', 'e', 'f', 'f', 'g', 'g', 'h', 'h']
    #     Enum.shuffle(board);
    # end

    # def flip do 
    # end

    # def restart(game) do 
    #     GenServer.call(reg(game), :restart);
    # end

end